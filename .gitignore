# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
# 앵커가 없으면 frontend/src/lib/ 까지 삼킨다. Python 빌드 산출물만 노린다.
/lib/
/lib64/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual Environment
venv/
ENV/
env/
.venv

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Environment variables
.env
.env.local
.env.*.local

# OS
.DS_Store
Thumbs.db

# 외부 API 자격증명·응답 캐시
# 자격증명이 실수로 커밋되는 것을 막는 광범위 규칙.
# 정당한 데이터 파일은 아래에서 명시적으로 예외 처리한다.
*.json
!docs/**/*.json
!backend/data/*.json
# 프론트 설정. 이게 없으면 클론해서 npm install 이 되지 않는다.
!frontend/*.json
# 지표 인덱스와 체크포인트는 Google Maps 콘텐츠다. stations.json(국토수치정보,
# 출처 표기 조건으로 재배포 가능)과 달리 공개 저장소에 올릴 수 없고,
# 30일 캐시 제한이 적용될 가능성도 있다.
backend/data/metrics.json
backend/data/korean_shops.json
# MLIT 취득 데이터. 이용 목적 회신대로 집계값만 쓰고 재배포하지 않는다 (스펙 §3.1.2).
backend/data/mlit_childcare.json
backend/data/mlit_prices.json
backend/data/.cache/
gcloud-service-key.json

# Node
node_modules/
.next/
out/
dist/

# Testing
.pytest_cache/
.coverage
htmlcov/

# Worktrees / SDD scratch
.claude/worktrees/
.superpowers/

# Next.js
frontend/.next/
frontend/node_modules/
frontend/out/
